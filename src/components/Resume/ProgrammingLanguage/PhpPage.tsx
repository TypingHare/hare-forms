import { Typography } from '@mui/material'
import { ResumeMainPage } from '../ResumeMainPage'

export function PhpPage() {
    return (
        <ResumeMainPage index="Programming_Language/PHP">
            <Typography>
                PHP was my first love, but we broke up after I knew that she was worse than Java in
                terms of performance, and had no way to deal with high concurrency.
            </Typography>
        </ResumeMainPage>
    )
}
