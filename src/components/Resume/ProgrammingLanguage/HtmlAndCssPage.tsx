import { Typography } from '@mui/material'
import { ResumeMainPage } from '../ResumeMainPage'

export function HtmlAndCssPage() {
    return (
        <ResumeMainPage index="Programming_Language/HTML_&_CSS">
            <Typography>
                Honestly I don't think I am good at HTML and CSS. There are too many tags,
                attributes, styles to memorize. Plus, I am not really into UI design.
            </Typography>
        </ResumeMainPage>
    )
}
